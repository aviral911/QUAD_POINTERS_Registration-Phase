'use client'

import { useEffect, useMemo, useState } from 'react'
import { supabase } from '@/lib/supabase-browser'
import {
  ArrowRight,
  Award,
  CalendarDays,
  Check,
  Clock3,
  Code2,
  Filter,
  GitBranch,
  Globe2,
  Layers3,
  MapPin,
  Menu,
  Search,
  Sparkles,
  Trophy,
  UploadCloud,
  Users,
  X,
} from 'lucide-react'

const hackathons = [
  { id: 'neon', title: 'Neon Future', subtitle: 'Build what comes next', date: 'Oct 18–20, 2026', location: 'Virtual + New York', tags: ['AI', 'Climate', 'Open source'], color: 'from-indigo-600 to-cyan-400', spots: '1,240 builders', status: 'Open now', days: '34' },
  { id: 'orbit', title: 'Orbit Labs', subtitle: 'Design for the edge', date: 'Nov 07–09, 2026', location: 'Virtual', tags: ['Web3', 'Design', 'Fintech'], color: 'from-fuchsia-600 to-orange-400', spots: '800 builders', status: 'Early access', days: '54' },
  { id: 'terra', title: 'Terra Nova', subtitle: 'Technology for a living planet', date: 'Dec 05–07, 2026', location: 'London + Virtual', tags: ['Sustainability', 'Hardware'], color: 'from-emerald-600 to-lime-400', spots: '620 builders', status: 'Opening soon', days: '82' },
]

type FormData = { name: string; email: string; role: string; experience: string; github: string; portfolio: string; motivation: string }
const initialForm: FormData = { name: '', email: '', role: 'Builder', experience: 'Intermediate', github: '', portfolio: '', motivation: '' }

function Brand() {
  return <a href="#top" className="flex items-center gap-2.5 font-semibold tracking-tight text-slate-950"><span className="grid size-9 place-items-center rounded-xl bg-slate-950 text-white shadow-lg shadow-indigo-200"><Sparkles className="size-4" /></span><span className="text-lg">forge<span className="text-indigo-600">/</span>events</span></a>
}

function Header({ onRegister }: { onRegister: () => void }) {
  const [open, setOpen] = useState(false)
  return <header className="sticky top-0 z-30 border-b border-slate-200/70 bg-white/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Brand /><nav className="hidden items-center gap-8 text-sm font-medium text-slate-500 md:flex"><a href="#discover" className="transition hover:text-slate-950">Discover</a><a href="#how-it-works" className="transition hover:text-slate-950">How it works</a><a href="#status" className="transition hover:text-slate-950">Check status</a></nav><div className="hidden items-center gap-4 md:flex"><a href="/auth" className="text-sm font-semibold text-slate-600 hover:text-slate-950">Sign in</a><a href="#status" className="text-sm font-semibold text-slate-600 hover:text-slate-950">Already registered?</a><button onClick={onRegister} className="rounded-full bg-slate-950 px-5 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-indigo-700">Apply to hackathon <ArrowRight className="ml-1 inline size-4" /></button></div><button aria-label="Open menu" onClick={() => setOpen(!open)} className="rounded-lg p-2 text-slate-600 md:hidden"><Menu className="size-5" /></button></div>{open && <div className="border-t border-slate-100 bg-white px-5 py-4 md:hidden"><div className="flex flex-col gap-4 text-sm font-semibold"><a href="#discover" onClick={() => setOpen(false)}>Discover</a><a href="#how-it-works" onClick={() => setOpen(false)}>How it works</a><a href="#status" onClick={() => setOpen(false)}>Check status</a><button onClick={onRegister} className="rounded-xl bg-slate-950 px-4 py-3 text-left text-white">Apply to hackathon <ArrowRight className="ml-1 inline size-4" /></button></div></div>}</header>
}

function Hero({ onRegister }: { onRegister: () => void }) {
  return <section id="top" className="relative overflow-hidden bg-[#f7f8fc]"><div className="absolute -left-32 top-0 size-96 rounded-full bg-indigo-200/35 blur-3xl" /><div className="absolute right-0 top-20 size-80 rounded-full bg-cyan-200/40 blur-3xl" /><div className="relative mx-auto grid max-w-7xl gap-10 px-5 pb-20 pt-16 lg:grid-cols-[1.1fr_.9fr] lg:items-center lg:px-8 lg:pb-28 lg:pt-24"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-indigo-100 bg-white/80 px-3 py-1.5 text-xs font-semibold uppercase tracking-[.16em] text-indigo-700 shadow-sm"><span className="size-1.5 rounded-full bg-emerald-500" />Applications open for fall 2026</div><h1 className="max-w-3xl text-5xl font-semibold leading-[1.02] tracking-[-.055em] text-slate-950 sm:text-6xl lg:text-7xl">Make something<br /><span className="bg-gradient-to-r from-indigo-600 via-violet-600 to-cyan-500 bg-clip-text text-transparent">worth building.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-600">Find your next challenge, meet your people, and ship an idea that moves the world forward.</p><div className="mt-9 flex flex-wrap gap-3"><button onClick={onRegister} className="rounded-full bg-indigo-600 px-6 py-3.5 text-sm font-semibold text-white shadow-xl shadow-indigo-200 transition hover:bg-indigo-700">Explore hackathons <ArrowRight className="ml-2 inline size-4" /></button><a href="#how-it-works" className="rounded-full border border-slate-300 bg-white px-6 py-3.5 text-sm font-semibold text-slate-700 transition hover:border-slate-400">How it works</a></div><div className="mt-12 flex gap-8 border-t border-slate-200 pt-5 text-sm"><div><p className="text-2xl font-semibold text-slate-950">12k<span className="text-indigo-600">+</span></p><p className="text-slate-500">builders registered</p></div><div><p className="text-2xl font-semibold text-slate-950">84</p><p className="text-slate-500">ideas shipped</p></div><div><p className="text-2xl font-semibold text-slate-950">$240k</p><p className="text-slate-500">in prizes</p></div></div></div><div className="relative mx-auto w-full max-w-md"><div className="absolute -right-3 -top-5 z-10 rounded-2xl border border-white/80 bg-white/90 p-4 shadow-xl shadow-indigo-100 backdrop-blur"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><Trophy className="size-4" /></div><div><p className="text-xs font-semibold text-slate-500">Next big thing</p><p className="text-sm font-semibold text-slate-950">Could be yours</p></div></div></div><div className="overflow-hidden rounded-[2rem] border border-white bg-gradient-to-br from-slate-950 via-indigo-950 to-indigo-700 p-6 shadow-2xl shadow-indigo-200"><div className="mb-20 flex items-center justify-between"><span className="rounded-full border border-white/20 bg-white/10 px-3 py-1 text-xs font-medium text-indigo-100">01 / 03</span><Layers3 className="size-5 text-cyan-300" /></div><div className="max-w-xs"><p className="mb-3 text-sm font-medium text-cyan-300">Featured challenge</p><h2 className="text-4xl font-semibold tracking-tight text-white">Neon<br />Future</h2><p className="mt-4 text-sm leading-6 text-indigo-100">Build tools for a brighter, more human tomorrow.</p></div><div className="mt-12 flex items-end justify-between"><div><p className="text-xs text-indigo-200">Prize pool</p><p className="mt-1 text-xl font-semibold text-white">$50,000</p></div><button onClick={onRegister} className="grid size-12 place-items-center rounded-full bg-white text-slate-950 transition hover:bg-cyan-300"><ArrowRight className="size-5" /></button></div></div></div></div></section>
}

function HackathonCard({ item, onApply }: { item: typeof hackathons[number]; onApply: () => void }) {
  return <article className="group overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl hover:shadow-slate-200/70"><div className={`relative h-36 bg-gradient-to-br ${item.color} p-5`}><div className="absolute inset-0 opacity-20" style={{ backgroundImage: 'radial-gradient(circle at 20% 20%, white 1px, transparent 1px)', backgroundSize: '18px 18px' }} /><div className="relative flex items-start justify-between"><span className="rounded-full bg-white/20 px-3 py-1 text-xs font-semibold text-white backdrop-blur">{item.status}</span><span className="rounded-full bg-black/15 px-3 py-1 text-xs font-medium text-white">in {item.days} days</span></div><div className="relative mt-8 text-white"><p className="text-xl font-semibold tracking-tight">{item.title}</p><p className="text-sm text-white/75">{item.subtitle}</p></div></div><div className="p-5"><div className="flex items-center gap-2 text-sm text-slate-600"><CalendarDays className="size-4 text-indigo-500" />{item.date}</div><div className="mt-2 flex items-center gap-2 text-sm text-slate-600"><MapPin className="size-4 text-indigo-500" />{item.location}</div><div className="mt-4 flex flex-wrap gap-1.5">{item.tags.map(tag => <span key={tag} className="rounded-md bg-slate-100 px-2 py-1 text-xs font-medium text-slate-600">{tag}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="flex items-center gap-1.5 text-xs font-medium text-slate-500"><Users className="size-3.5" />{item.spots}</span><button onClick={onApply} className="text-sm font-semibold text-indigo-600 transition group-hover:text-indigo-800">View details <ArrowRight className="ml-1 inline size-3.5" /></button></div></div></article>
}

function Discover({ onRegister }: { onRegister: () => void }) {
  const [query, setQuery] = useState('')
  const filtered = useMemo(() => hackathons.filter(item => `${item.title} ${item.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())), [query])
  return <section id="discover" style={{ contentVisibility: 'auto', containIntrinsicSize: '720px' }} className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="mb-3 text-sm font-semibold uppercase tracking-[.16em] text-indigo-600">The good stuff</p><h2 className="text-3xl font-semibold tracking-tight text-slate-950 sm:text-4xl">Find your next build.</h2><p className="mt-3 text-slate-500">Curated challenges for curious minds.</p></div><div className="flex w-full gap-2 md:w-auto"><label className="relative flex-1 md:w-64"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search hackathons" className="h-11 w-full rounded-xl border border-slate-200 bg-slate-50 pl-9 pr-3 text-sm outline-none ring-indigo-200 transition focus:bg-white focus:ring-4" /></label><button aria-label="Filter hackathons" className="grid size-11 place-items-center rounded-xl border border-slate-200 bg-white text-slate-600"><Filter className="size-4" /></button></div></div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(item => <HackathonCard key={item.id} item={item} onApply={onRegister} />)}</div></section>
}

function HowItWorks() {
  const steps = [{ icon: Search, title: 'Choose a challenge', text: 'Find a hackathon that makes your brain light up.' }, { icon: Users, title: 'Bring your people', text: 'Build solo or invite collaborators who see it too.' }, { icon: Code2, title: 'Ship something real', text: 'Turn a sharp idea into a working prototype.' }]
  return <section id="how-it-works" style={{ contentVisibility: 'auto', containIntrinsicSize: '520px' }} className="border-y border-slate-200/80 bg-slate-50/75"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="grid gap-12 lg:grid-cols-[.7fr_1.3fr] lg:items-start"><div><p className="mb-3 text-sm font-semibold uppercase tracking-[.16em] text-indigo-600">No gatekeeping</p><h2 className="text-3xl font-semibold tracking-tight text-slate-950 sm:text-4xl">From spark to<br /><span className="text-indigo-600">shipped.</span></h2></div><div className="grid gap-8 sm:grid-cols-3">{steps.map((step, i) => <div key={step.title} className="relative"><div className="mb-5 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-white text-indigo-600 shadow-sm ring-1 ring-slate-200"><step.icon className="size-5" /></div><span className="text-xs font-semibold text-slate-400">0{i + 1}</span></div><h3 className="font-semibold text-slate-950">{step.title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{step.text}</p></div>)}</div></div></div></section>
}

function StatusLookup() {
  const [email, setEmail] = useState('')
  const [result, setResult] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function lookupStatus(event: React.FormEvent) {
    event.preventDefault()
    setLoading(true)
    const { data, error } = await supabase.from('registrations').select('status').eq('email', email.trim().toLowerCase()).order('created_at', { ascending: false }).limit(1).maybeSingle()
    setResult(error ? 'Unable to check right now.' : data ? `Application status: ${data.status.replace('_', ' ')}` : 'No application found for that email.')
    setLoading(false)
  }

  return <section id="status" style={{ contentVisibility: 'auto', containIntrinsicSize: '360px' }} className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="overflow-hidden rounded-[2rem] bg-slate-950 px-6 py-10 text-white sm:px-12 lg:flex lg:items-center lg:justify-between lg:px-16"><div><p className="mb-3 text-sm font-semibold uppercase tracking-[.16em] text-cyan-300">Your application, your way</p><h2 className="text-3xl font-semibold tracking-tight sm:text-4xl">Check your status.</h2><p className="mt-3 max-w-md text-sm leading-6 text-slate-400">Enter the email you applied with to see the latest update from our team.</p></div><div className="mt-8 w-full max-w-md lg:mt-0"><form onSubmit={lookupStatus} className="flex gap-2"><input type="email" required value={email} onChange={e => setEmail(e.target.value)} placeholder="you@example.com" className="min-w-0 flex-1 rounded-xl border border-white/15 bg-white/10 px-4 py-3 text-sm text-white outline-none placeholder:text-slate-500 focus:border-cyan-300" /><button disabled={loading} className="rounded-xl bg-cyan-300 px-4 py-3 text-sm font-semibold text-slate-950 disabled:opacity-60">{loading ? 'Checking...' : 'Look up'}</button></form>{result && <div className="mt-3 flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />{result}</div>}</div></div></section>
}

function RegistrationModal({ onClose }: { onClose: () => void }) {
  const [step, setStep] = useState(1)
  const [submitted, setSubmitted] = useState(false)
  const [error, setError] = useState('')
  const [data, setData] = useState<FormData>(initialForm)
  const update = (key: keyof FormData, value: string) => setData(prev => ({ ...prev, [key]: value }))
  const submitApplication = async () => {
    setError('')
    const { data: hackathon, error: hackathonError } = await supabase.from('hackathons').select('id').eq('title', 'Neon Future').eq('status', 'open').maybeSingle()
    if (hackathonError || !hackathon) {
      setError('The hackathon is not available yet. Please try again soon.')
      return
    }
    const { error: registrationError } = await supabase.from('registrations').insert({ hackathon_id: hackathon.id, full_name: data.name.trim(), email: data.email.trim().toLowerCase(), role: data.role, experience: data.experience, github_url: data.github || null, portfolio_url: data.portfolio || null, motivation: data.motivation || null })
    if (registrationError) {
      setError(registrationError.code === '23505' ? 'This email is already registered for the hackathon.' : 'We could not save your application. Please try again.')
      return
    }
    setSubmitted(true)
  }
  if (submitted) return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl bg-white p-8 text-center shadow-2xl"><div className="mx-auto grid size-16 place-items-center rounded-full bg-emerald-100 text-emerald-600"><Check className="size-8" /></div><h2 className="mt-5 text-2xl font-semibold text-slate-950">You&apos;re on the list.</h2><p className="mt-2 text-sm leading-6 text-slate-500">We&apos;ll send next steps to {data.email || 'your inbox'} shortly. Good luck building something brilliant.</p><button onClick={onClose} className="mt-7 w-full rounded-xl bg-slate-950 py-3 text-sm font-semibold text-white">Done</button></div></div>
  const labels = ['About you', 'Your work', 'Motivation', 'Review']
  return <div role="dialog" aria-modal="true" aria-label="Hackathon application" className="fixed inset-0 z-50 overflow-y-auto bg-slate-950/50 p-4 backdrop-blur-sm sm:p-8"><div className="mx-auto my-4 max-w-2xl rounded-3xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-100 px-6 py-5 sm:px-8"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-indigo-600">Neon Future 2026</p><h2 className="mt-1 text-xl font-semibold text-slate-950">Apply to build</h2></div><button aria-label="Close application" onClick={onClose} className="rounded-full p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-700"><X className="size-5" /></button></div><div className="px-6 pt-6 sm:px-8"><div className="flex justify-between">{labels.map((label, i) => <div key={label} className={`flex items-center gap-2 text-xs font-semibold ${step >= i + 1 ? 'text-indigo-600' : 'text-slate-400'}`}><span className={`grid size-7 place-items-center rounded-full text-xs ${step >= i + 1 ? 'bg-indigo-600 text-white' : 'bg-slate-100'}`}>{step > i + 1 ? <Check className="size-3.5" /> : i + 1}</span><span className="hidden sm:inline">{label}</span></div>)}</div><div className="mt-3 h-1 rounded-full bg-slate-100"><div className="h-1 rounded-full bg-indigo-600 transition-all" style={{ width: `${(step / 4) * 100}%` }} /></div></div><div className="px-6 py-7 sm:px-8">{error && <p role="alert" className="mb-5 rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}{step === 1 && <div className="space-y-5"><div><h3 className="text-lg font-semibold text-slate-950">First, tell us about yourself.</h3><p className="mt-1 text-sm text-slate-500">A few basics so we know who&apos;s joining the room.</p></div><label className="block text-sm font-medium text-slate-700">Full name<input value={data.name} onChange={e => update('name', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" placeholder="Ada Lovelace" /></label><label className="block text-sm font-medium text-slate-700">Email address<input type="email" value={data.email} onChange={e => update('email', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" placeholder="ada@example.com" /></label><div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-medium text-slate-700">I&apos;m a...<select value={data.role} onChange={e => update('role', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 outline-none focus:border-indigo-500"><option>Builder</option><option>Designer</option><option>Product thinker</option><option>Student</option></select></label><label className="block text-sm font-medium text-slate-700">Experience<select value={data.experience} onChange={e => update('experience', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 outline-none focus:border-indigo-500"><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></label></div></div>}{step === 2 && <div className="space-y-5"><div><h3 className="text-lg font-semibold text-slate-950">Show us your playground.</h3><p className="mt-1 text-sm text-slate-500">Links are optional, but they help us get to know your work.</p></div><label className="block text-sm font-medium text-slate-700"><GitBranch className="mr-2 inline size-4" />GitHub profile<input value={data.github} onChange={e => update('github', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" placeholder="github.com/yourname" /></label><label className="block text-sm font-medium text-slate-700"><Globe2 className="mr-2 inline size-4" />Portfolio or LinkedIn<input value={data.portfolio} onChange={e => update('portfolio', e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" placeholder="yourwebsite.com" /></label><div className="rounded-2xl border-2 border-dashed border-slate-200 p-7 text-center transition hover:border-indigo-300"><UploadCloud className="mx-auto size-8 text-indigo-500" /><p className="mt-2 text-sm font-medium text-slate-700">Drop a resume here</p><p className="mt-1 text-xs text-slate-400">PDF or DOCX, up to 10MB</p></div></div>}{step === 3 && <div className="space-y-5"><div><h3 className="text-lg font-semibold text-slate-950">What would you love to make?</h3><p className="mt-1 text-sm text-slate-500">There&apos;s no wrong answer. We&apos;re looking for curiosity.</p></div><textarea value={data.motivation} onChange={e => update('motivation', e.target.value)} rows={7} className="w-full resize-none rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" placeholder="Tell us about an idea you can't stop thinking about..." /><div className="rounded-xl bg-indigo-50 p-4 text-sm leading-6 text-indigo-800"><Sparkles className="mr-2 inline size-4" />Tip: Great answers show us how you think, not just what you&apos;ve done.</div></div>}{step === 4 && <div className="space-y-4"><div><h3 className="text-lg font-semibold text-slate-950">Looks good?</h3><p className="mt-1 text-sm text-slate-500">Review your application before sending it into the world.</p></div><div className="divide-y divide-slate-100 rounded-2xl border border-slate-200">{[['Name', data.name || 'Not provided'], ['Email', data.email || 'Not provided'], ['Role', data.role], ['Experience', data.experience], ['GitHub', data.github || 'Not provided'], ['Motivation', data.motivation || 'Not provided']].map(([key, value]) => <div key={key} className="flex justify-between gap-4 px-4 py-3 text-sm"><span className="text-slate-500">{key}</span><span className="max-w-[65%] truncate font-medium text-slate-900">{value}</span></div>)}</div><p className="text-xs leading-5 text-slate-400">By submitting, you agree to receive event updates from forge/events. You can unsubscribe anytime.</p></div>}<div className="mt-8 flex justify-between border-t border-slate-100 pt-5"><button onClick={() => step > 1 ? setStep(step - 1) : onClose()} className="rounded-xl px-4 py-3 text-sm font-semibold text-slate-600 hover:bg-slate-100">{step === 1 ? 'Cancel' : 'Back'}</button><button disabled={step === 1 && (!data.name || !data.email)} onClick={() => step < 4 ? setStep(step + 1) : setSubmitted(true)} className="rounded-xl bg-indigo-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-indigo-200 transition hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-40">{step === 4 ? 'Submit application' : 'Continue'} <ArrowRight className="ml-1 inline size-4" /></button></div></div></div></div>
}

export default function HackathonPortal() {
  const [registering, setRegistering] = useState(false)
  const [isAuthenticated, setIsAuthenticated] = useState(false)

  useEffect(() => {
    let mounted = true
    supabase.auth.getSession().then(({ data }) => {
      if (mounted) setIsAuthenticated(Boolean(data.session))
    })

    const { data: authListener } = supabase.auth.onAuthStateChange((_event, session) => {
      setIsAuthenticated(Boolean(session))
    })

    return () => {
      mounted = false
      authListener.subscription.unsubscribe()
    }
  }, [])

  const handleRegister = () => {
    if (!isAuthenticated) {
      window.location.href = '/auth'
      return
    }
    setRegistering(true)
  }

  return <div className="min-h-screen bg-white text-slate-950"><Header onRegister={handleRegister} /><main><Hero onRegister={handleRegister} /><Discover onRegister={handleRegister} /><HowItWorks /><StatusLookup /></main><footer className="border-t border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-8 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between lg:px-8"><Brand /><p>Built for the people who keep building.</p><a href="/admin-portal-xyz" className="text-xs text-slate-400 hover:text-slate-700">Admin portal</a></div></footer>{registering && isAuthenticated && <RegistrationModal onClose={() => setRegistering(false)} />}</div>
}

export function AdminPortal() {
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState('All statuses')
  const candidates = [{ name: 'Maya Chen', email: 'maya.chen@email.com', role: 'Builder', challenge: 'Neon Future', applied: '2 hours ago', status: 'Under review' }, { name: 'Jordan Brooks', email: 'jordan.brooks@email.com', role: 'Designer', challenge: 'Orbit Labs', applied: 'Yesterday', status: 'Accepted' }, { name: 'Samira Patel', email: 'samira.patel@email.com', role: 'Product thinker', challenge: 'Neon Future', applied: 'Yesterday', status: 'New' }, { name: 'Theo Martin', email: 'theo.martin@email.com', role: 'Builder', challenge: 'Terra Nova', applied: '3 days ago', status: 'Waitlisted' }]
  const filtered = candidates.filter(c => `${c.name} ${c.email} ${c.challenge}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All statuses' || c.status === status))
  return <div className="min-h-screen bg-[#f7f8fc] text-slate-950"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Brand /><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold text-indigo-700">Admin workspace</span></div></header><main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-indigo-600">Good morning, admin</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Applications overview</h1></div><a href="#new" className="rounded-xl bg-slate-950 px-4 py-3 text-center text-sm font-semibold text-white">+ New hackathon</a></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5"><Users className="size-5 text-indigo-500" /><p className="mt-5 text-3xl font-semibold">248</p><p className="mt-1 text-sm text-slate-500">Total applicants</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><Clock3 className="size-5 text-amber-500" /><p className="mt-5 text-3xl font-semibold">86</p><p className="mt-1 text-sm text-slate-500">Awaiting review</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><Award className="size-5 text-emerald-500" /><p className="mt-5 text-3xl font-semibold">42</p><p className="mt-1 text-sm text-slate-500">Accepted this week</p></div></div><section className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="flex flex-col gap-3 border-b border-slate-200 p-5 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search candidates" className="h-10 w-full rounded-lg border border-slate-200 pl-9 pr-3 text-sm outline-none focus:border-indigo-500" /></label><select value={status} onChange={e => setStatus(e.target.value)} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm"><option>All statuses</option><option>New</option><option>Under review</option><option>Accepted</option><option>Waitlisted</option></select><button className="rounded-lg border border-slate-200 px-4 text-sm font-medium hover:bg-slate-50">Export CSV</button></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3 font-semibold">Candidate</th><th className="px-5 py-3 font-semibold">Challenge</th><th className="px-5 py-3 font-semibold">Applied</th><th className="px-5 py-3 font-semibold">Status</th><th className="px-5 py-3 font-semibold">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(c => <tr key={c.email} className="hover:bg-slate-50/80"><td className="px-5 py-4"><p className="font-semibold text-slate-900">{c.name}</p><p className="mt-0.5 text-xs text-slate-500">{c.email} · {c.role}</p></td><td className="px-5 py-4 text-slate-600">{c.challenge}</td><td className="px-5 py-4 text-slate-500">{c.applied}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${c.status === 'Accepted' ? 'bg-emerald-50 text-emerald-700' : c.status === 'New' ? 'bg-blue-50 text-blue-700' : c.status === 'Waitlisted' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{c.status}</span></td><td className="px-5 py-4"><button className="text-sm font-semibold text-indigo-600 hover:text-indigo-800">View <ArrowRight className="ml-1 inline size-3.5" /></button></td></tr>)}</tbody></table></div></section></main></div>
}

export { hackathons }
